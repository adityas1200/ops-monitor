# Optional — enables the Claude harness so agents reason over their skill files.
# Without it, agents use deterministic fallbacks (platform stays fully functional).
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-sonnet-4-6
